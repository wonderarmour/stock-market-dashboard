{
  "name": "market-dashboard",
  "version": "1.0.0",
  "description": "환율·지수·뉴스·과거 이벤트·포트폴리오 대시보드 (Node 내장 모듈만 사용, 외부 의존성 없음)",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/chat.js && node --check public/news.js && node --check public/drawdown.js && node --check public/portfolio.js"
  },
  "engines": {
    "node": ">=20.12.0"
  },
  "license": "UNLICENSED"
}
